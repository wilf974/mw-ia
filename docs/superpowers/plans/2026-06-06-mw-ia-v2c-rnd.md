# V2-C0 — RND minimal (exploration intrinsèque apprise) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer l'oracle count-based de BX par un bonus de nouveauté appris (RND single-stream) ajouté au reward extrinsèque, derrière un flag `default=off`, sans toucher l'agent DQN.

**Architecture:** Un `RNDModule` isolé (deux petits CNN — target figée + predictor entraîné — plus normaliseurs d'obs et de bonus) possédé par `ConvRecurrentProceduralDQNRunner`. À chaque pas : `reward_total = reward_ext + beta * clip(rnd_bonus_norm)`. La normalisation par running-std rend le bonus O(1) (β robuste) ; clip per-step + ratio loggé + WARN forment le garde-fou ; warmup laisse les normaliseurs se caler. No-op strict quand désactivé → baselines reproductibles bit-à-bit.

**Tech Stack:** Python 3.13, PyTorch 2.11+cu128, numpy, pytest. Conventions projet : TDD test-first, flags opt-in (pattern `bx_*`), messages/help-text ASCII (piège #8 Windows cp1252).

**Spec de référence :** `docs/superpowers/specs/2026-06-06-mw-ia-v2c-rnd-design.md`

**Pré-requis :** `source .venv/Scripts/activate` avant pytest. Baseline tests : `pytest -q` → **384 passed** au départ.

---

## Task 1 : `RunningMeanStd` — normaliseur en ligne

**Files:**
- Create: `mw_ia/neural/running_mean_std.py`
- Test: `tests/neural/test_running_mean_std.py` (créer)

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/neural/test_running_mean_std.py` :

```python
"""Tests du normaliseur en ligne RunningMeanStd (V2-C0 RND)."""
from __future__ import annotations

import numpy as np

from mw_ia.neural.running_mean_std import RunningMeanStd


def test_mean_converges_scalar():
    rms = RunningMeanStd(shape=())
    rng = np.random.RandomState(0)
    data = rng.normal(5.0, 2.0, size=5000)
    for i in range(0, 5000, 50):
        rms.update(data[i : i + 50])
    assert abs(rms.mean - 5.0) < 0.2
    assert abs(rms.std - 2.0) < 0.2


def test_var_non_negative_single_samples():
    rms = RunningMeanStd(shape=())
    for x in (3.0, 3.0, 3.0):
        rms.update(np.array([x]))
    assert rms.var >= 0.0
    assert rms.std >= 0.0


def test_vector_shape_tracked_elementwise():
    rms = RunningMeanStd(shape=(2,))
    batch = np.array([[1.0, 10.0], [3.0, 30.0]])
    rms.update(batch)
    assert rms.mean.shape == (2,)
    assert rms.mean[1] > rms.mean[0]


def test_std_positive_after_init():
    rms = RunningMeanStd(shape=(3, 4, 4))
    assert np.all(rms.std > 0.0)
```

- [ ] **Step 2: Lancer pour vérifier l'échec**

Run: `pytest tests/neural/test_running_mean_std.py -q`
Expected: FAIL (`ModuleNotFoundError: mw_ia.neural.running_mean_std`).

- [ ] **Step 3: Implémenter**

Créer `mw_ia/neural/running_mean_std.py` :

```python
"""RunningMeanStd — moyenne/variance en ligne (algorithme parallele de Chan).

Utilise par V2-C0 RND pour normaliser les observations et les bonus
intrinseques. numpy float64, hors graphe torch.
"""
from __future__ import annotations

import numpy as np


class RunningMeanStd:
    """Moyenne et variance courantes mises a jour par batch.

    update(x) : x de shape (batch, *shape). Les statistiques sont suivies
    element-wise sur `shape`.
    """

    def __init__(self, shape: tuple[int, ...] = ()) -> None:
        self.mean = np.zeros(shape, dtype=np.float64)
        self.var = np.ones(shape, dtype=np.float64)
        self.count = 1e-4

    def update(self, x: np.ndarray) -> None:
        x = np.asarray(x, dtype=np.float64)
        batch_mean = x.mean(axis=0)
        batch_var = x.var(axis=0)
        batch_count = x.shape[0]
        self._update_from_moments(batch_mean, batch_var, batch_count)

    def _update_from_moments(
        self, batch_mean: np.ndarray, batch_var: np.ndarray, batch_count: int
    ) -> None:
        delta = batch_mean - self.mean
        tot = self.count + batch_count
        new_mean = self.mean + delta * batch_count / tot
        m_a = self.var * self.count
        m_b = batch_var * batch_count
        m2 = m_a + m_b + np.square(delta) * self.count * batch_count / tot
        self.var = m2 / tot
        self.mean = new_mean
        self.count = tot

    @property
    def std(self) -> np.ndarray:
        return np.sqrt(self.var)
```

- [ ] **Step 4: Lancer pour vérifier le succès**

Run: `pytest tests/neural/test_running_mean_std.py -q`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add mw_ia/neural/running_mean_std.py tests/neural/test_running_mean_std.py
git commit -m "feat(v2-c0): RunningMeanStd online normalizer for RND"
```

---

## Task 2 : `_RNDNet` — petit CNN target/predictor

**Files:**
- Create: `mw_ia/neural/rnd.py` (première brique : la classe réseau)
- Test: `tests/neural/test_rnd_net.py` (créer)

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/neural/test_rnd_net.py` :

```python
"""Tests du reseau _RNDNet (V2-C0 RND)."""
from __future__ import annotations

import torch

from mw_ia.neural.rnd import _RNDNet


def test_output_shape():
    net = _RNDNet(in_channels=3, rows=4, cols=4, embed_dim=8)
    x = torch.zeros(2, 3, 4, 4)
    out = net(x)
    assert out.shape == (2, 8)


def test_two_nets_differ_on_random_init():
    torch.manual_seed(0)
    a = _RNDNet(in_channels=3, rows=4, cols=4, embed_dim=8)
    b = _RNDNet(in_channels=3, rows=4, cols=4, embed_dim=8)
    x = torch.ones(1, 3, 4, 4)
    assert not torch.allclose(a(x), b(x))
```

- [ ] **Step 2: Lancer pour vérifier l'échec**

Run: `pytest tests/neural/test_rnd_net.py -q`
Expected: FAIL (`ImportError: cannot import name '_RNDNet'`).

- [ ] **Step 3: Implémenter**

Créer `mw_ia/neural/rnd.py` :

```python
"""RND (Random Network Distillation, Burda et al. 2018) — exploration intrinseque.

V2-C0 single-stream : le bonus = erreur de prediction d'un predictor entraine
a imiter une target figee a init aleatoire. Etats nouveaux -> grosse erreur ->
gros bonus. Le bonus normalise est ajoute au reward extrinseque dans le runner.

Voir spec : docs/superpowers/specs/2026-06-06-mw-ia-v2c-rnd-design.md
"""
from __future__ import annotations

import numpy as np
import torch
from torch import nn

from mw_ia.neural.running_mean_std import RunningMeanStd


class _RNDNet(nn.Module):
    """Petit CNN (C,R,Cc) -> embedding. Reutilise pour target (figee) et predictor."""

    def __init__(self, *, in_channels: int, rows: int, cols: int, embed_dim: int) -> None:
        super().__init__()
        self.net = nn.Sequential(
            nn.Conv2d(in_channels, 16, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.Conv2d(16, 32, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.Flatten(),
            nn.Linear(32 * rows * cols, embed_dim),
        )

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        return self.net(x)
```

Note : `_RNDNet.__init__` est keyword-only (`*`). Le test l'appelle en kwargs.

- [ ] **Step 4: Lancer pour vérifier le succès**

Run: `pytest tests/neural/test_rnd_net.py -q`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add mw_ia/neural/rnd.py tests/neural/test_rnd_net.py
git commit -m "feat(v2-c0): _RNDNet small CNN for target/predictor"
```

---

## Task 3 : `RNDModule` — bonus, update, warmup, clip, normalisation

**Files:**
- Modify: `mw_ia/neural/rnd.py` (ajouter la classe `RNDModule` après `_RNDNet`)
- Test: `tests/neural/test_rnd_module.py` (créer)

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/neural/test_rnd_module.py` :

```python
"""Tests de RNDModule (V2-C0 RND) — proprietes centrales."""
from __future__ import annotations

import numpy as np
import torch

from mw_ia.neural.rnd import RNDModule


def _obs(seed: int) -> np.ndarray:
    return np.random.RandomState(seed).rand(3, 4, 4).astype(np.float32)


def test_predictor_error_decreases_on_repeated_obs():
    # Propriete RND centrale : le predictor apprend l'obs -> erreur baisse.
    mod = RNDModule(in_channels=3, rows=4, cols=4, warmup_steps=0, seed=0, device="cpu")
    x = _obs(0)
    losses = [mod.update(x) for _ in range(50)]
    assert losses[-1] < losses[0]


def test_bonus_non_negative():
    mod = RNDModule(in_channels=3, rows=4, cols=4, warmup_steps=0, seed=0, device="cpu")
    assert mod.compute_bonus(_obs(1)) >= 0.0


def test_warmup_returns_zero():
    mod = RNDModule(in_channels=3, rows=4, cols=4, warmup_steps=5, seed=0, device="cpu")
    x = _obs(2)
    for _ in range(5):
        assert mod.compute_bonus(x) == 0.0  # steps 1..5 <= warmup
    assert mod.compute_bonus(x) >= 0.0      # step 6 > warmup


def test_bonus_clipped():
    mod = RNDModule(
        in_channels=3, rows=4, cols=4, warmup_steps=0, clip=0.001, seed=0, device="cpu"
    )
    assert mod.compute_bonus(_obs(3)) <= 0.001


def test_target_is_frozen():
    mod = RNDModule(in_channels=3, rows=4, cols=4, warmup_steps=0, seed=0, device="cpu")
    before = [p.detach().clone() for p in mod.target.parameters()]
    x = _obs(4)
    for _ in range(10):
        mod.update(x)
    for b, p in zip(before, mod.target.parameters()):
        assert torch.equal(b, p)


def test_predictor_is_trained():
    mod = RNDModule(in_channels=3, rows=4, cols=4, warmup_steps=0, seed=0, device="cpu")
    before = [p.detach().clone() for p in mod.predictor.parameters()]
    x = _obs(5)
    for _ in range(10):
        mod.update(x)
    changed = any(
        not torch.equal(b, p) for b, p in zip(before, mod.predictor.parameters())
    )
    assert changed
```

- [ ] **Step 2: Lancer pour vérifier l'échec**

Run: `pytest tests/neural/test_rnd_module.py -q`
Expected: FAIL (`ImportError: cannot import name 'RNDModule'`).

- [ ] **Step 3: Implémenter**

Dans `mw_ia/neural/rnd.py`, ajouter après `_RNDNet` :

```python
class RNDModule:
    """RND single-stream : bonus de nouveaute appris, normalise et clippe.

    compute_bonus(obs) : met a jour le normaliseur d'obs, calcule l'erreur de
    prediction, la normalise par sa running-std, clippe, retourne 0 pendant le
    warmup. update(obs) : un pas de gradient sur le predictor.

    L'agent DQN ne connait pas RND : le runner ajoute beta*compute_bonus au reward.
    """

    def __init__(
        self,
        *,
        in_channels: int,
        rows: int,
        cols: int,
        embed_dim: int = 128,
        lr: float = 1e-4,
        clip: float = 5.0,
        warmup_steps: int = 1000,
        device: str = "cpu",
        seed: int = 0,
    ) -> None:
        torch.manual_seed(seed)
        self.device = torch.device(device)
        # target construite en premier, predictor ensuite -> inits aleatoires distinctes
        self.target = _RNDNet(
            in_channels=in_channels, rows=rows, cols=cols, embed_dim=embed_dim
        ).to(self.device)
        self.predictor = _RNDNet(
            in_channels=in_channels, rows=rows, cols=cols, embed_dim=embed_dim
        ).to(self.device)
        for p in self.target.parameters():
            p.requires_grad_(False)
        self.optimizer = torch.optim.Adam(self.predictor.parameters(), lr=lr)
        self.obs_rms = RunningMeanStd(shape=(in_channels, rows, cols))
        self.bonus_rms = RunningMeanStd(shape=())
        self.clip = clip
        self.warmup_steps = warmup_steps
        self._step = 0

    def _normalize_obs(self, obs: np.ndarray) -> np.ndarray:
        o = (obs - self.obs_rms.mean) / np.sqrt(self.obs_rms.var + 1e-8)
        return np.clip(o, -5.0, 5.0).astype(np.float32)

    def compute_bonus(self, obs: np.ndarray) -> float:
        self.obs_rms.update(obs[None])
        self._step += 1
        o = self._normalize_obs(obs)
        t = torch.from_numpy(o[None]).to(self.device)
        with torch.no_grad():
            err = float(((self.predictor(t) - self.target(t)) ** 2).mean().item())
        self.bonus_rms.update(np.array([err], dtype=np.float64))
        if self._step <= self.warmup_steps:
            return 0.0
        bonus = err / (float(self.bonus_rms.std) + 1e-8)
        return float(np.clip(bonus, 0.0, self.clip))

    def update(self, obs: np.ndarray) -> float:
        o = self._normalize_obs(obs)
        t = torch.from_numpy(o[None]).to(self.device)
        with torch.no_grad():
            tgt = self.target(t)
        pred = self.predictor(t)
        loss = ((pred - tgt) ** 2).mean()
        self.optimizer.zero_grad(set_to_none=True)
        loss.backward()
        self.optimizer.step()
        return float(loss.item())
```

- [ ] **Step 4: Lancer pour vérifier le succès**

Run: `pytest tests/neural/test_rnd_module.py -q`
Expected: PASS (6 tests).

- [ ] **Step 5: Commit**

```bash
git add mw_ia/neural/rnd.py tests/neural/test_rnd_module.py
git commit -m "feat(v2-c0): RNDModule (bonus + update + warmup + clip + normalization)"
```

---

## Task 4 : Flags config `rnd_*`

**Files:**
- Modify: `mw_ia/config.py` (`ConvRecurrentDQNConfig` : champs après ligne 432 ; validation à la fin de `__post_init__`)
- Test: `tests/test_rnd_config.py` (créer)

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/test_rnd_config.py` :

```python
"""Tests des flags V2-C0 RND sur ConvRecurrentDQNConfig."""
from __future__ import annotations

import pytest

from mw_ia.config import ConvRecurrentDQNConfig


def test_defaults_are_no_op():
    cfg = ConvRecurrentDQNConfig()
    assert cfg.rnd_enabled is False
    assert cfg.rnd_beta == 0.5
    assert cfg.rnd_lr == 1e-4
    assert cfg.rnd_embed_dim == 128
    assert cfg.rnd_clip == 5.0
    assert cfg.rnd_warmup_steps == 1000
    assert cfg.rnd_ratio_warn == 10.0


def test_enabled_accepted():
    cfg = ConvRecurrentDQNConfig(rnd_enabled=True, rnd_beta=0.2)
    assert cfg.rnd_enabled is True
    assert cfg.rnd_beta == 0.2


def test_negative_beta_rejected():
    with pytest.raises(ValueError, match="rnd_beta"):
        ConvRecurrentDQNConfig(rnd_beta=-0.1)


def test_non_positive_clip_rejected():
    with pytest.raises(ValueError, match="rnd_clip"):
        ConvRecurrentDQNConfig(rnd_clip=0.0)


def test_negative_warmup_rejected():
    with pytest.raises(ValueError, match="rnd_warmup_steps"):
        ConvRecurrentDQNConfig(rnd_warmup_steps=-1)


def test_non_positive_embed_rejected():
    with pytest.raises(ValueError, match="rnd_embed_dim"):
        ConvRecurrentDQNConfig(rnd_embed_dim=0)
```

- [ ] **Step 2: Lancer pour vérifier l'échec**

Run: `pytest tests/test_rnd_config.py -q`
Expected: FAIL (`TypeError: unexpected keyword argument 'rnd_enabled'`).

- [ ] **Step 3: Ajouter les champs + validation**

Dans `mw_ia/config.py`, `ConvRecurrentDQNConfig`, juste après la ligne `bx_novelty_beta: float = 0.0   # bonus exploration count-based (Sonde B)` (ligne 432), ajouter :

```python
    # V2-C0 : RND exploration intrinseque (single-stream, default = no-op)
    rnd_enabled: bool = False
    rnd_beta: float = 0.5
    rnd_lr: float = 1e-4
    rnd_embed_dim: int = 128
    rnd_clip: float = 5.0
    rnd_warmup_steps: int = 1000
    rnd_ratio_warn: float = 10.0
```

Puis à la fin de `ConvRecurrentDQNConfig.__post_init__` (après la dernière validation existante), ajouter :

```python
        if self.rnd_beta < 0.0:
            raise ValueError(f"rnd_beta doit etre >= 0, recu {self.rnd_beta}")
        if self.rnd_lr <= 0.0:
            raise ValueError(f"rnd_lr doit etre > 0, recu {self.rnd_lr}")
        if self.rnd_embed_dim <= 0:
            raise ValueError(f"rnd_embed_dim doit etre > 0, recu {self.rnd_embed_dim}")
        if self.rnd_clip <= 0.0:
            raise ValueError(f"rnd_clip doit etre > 0, recu {self.rnd_clip}")
        if self.rnd_warmup_steps < 0:
            raise ValueError(
                f"rnd_warmup_steps doit etre >= 0, recu {self.rnd_warmup_steps}"
            )
        if self.rnd_ratio_warn <= 0.0:
            raise ValueError(f"rnd_ratio_warn doit etre > 0, recu {self.rnd_ratio_warn}")
```

- [ ] **Step 4: Lancer pour vérifier le succès**

Run: `pytest tests/test_rnd_config.py -q`
Expected: PASS (6 tests).

- [ ] **Step 5: Non-régression config**

Run: `pytest tests/ -q -k config`
Expected: tous verts.

- [ ] **Step 6: Commit**

```bash
git add mw_ia/config.py tests/test_rnd_config.py
git commit -m "feat(v2-c0): rnd_* flags on ConvRecurrentDQNConfig"
```

---

## Task 5 : Wiring RND dans le runner + log `RND_RESULT`

**Files:**
- Modify: `mw_ia/training/runner.py` (`ConvRecurrentProceduralDQNRunner` : import, `__init__`, `run()`)
- Test: `tests/training/test_rnd_runner.py` (créer)

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/training/test_rnd_runner.py` :

```python
"""Tests d'integration V2-C0 RND runner."""
from __future__ import annotations

from mw_ia.config import (
    ConvRecurrentDQNConfig, ProceduralEnvConfig, SchedulerConfig, TrainingConfig,
)
from mw_ia.envs.maze_generators import RandomObstaclesGenerator
from mw_ia.envs.procedural_env import ProceduralGridWorld
from mw_ia.training.runner import ConvRecurrentProceduralDQNRunner, RunnerCallbacks


def _make_runner(on_log=None, **cfg_overrides):
    proc_cfg = ProceduralEnvConfig(mode="obstacles", max_rows=5, max_cols=5, max_steps=20)
    gen = RandomObstaclesGenerator(
        rows=5, cols=5, start=(0, 0), goal=(4, 4),
        min_density=proc_cfg.min_density, max_density=proc_cfg.max_density,
        max_attempts=100,
    )
    env = ProceduralGridWorld(cfg=proc_cfg, generator=gen)
    dqn_cfg = ConvRecurrentDQNConfig(
        episodes=3, conv_channels=(8,), lstm_hidden=16, sequence_length=4,
        replay_capacity=10, min_episodes_to_learn=1, batch_size=1,
        max_steps_per_episode=20, eval_max_steps=20, eval_enabled=False,
        **cfg_overrides,
    )
    cb = RunnerCallbacks(on_log=on_log)
    return ConvRecurrentProceduralDQNRunner(
        env=env, proc_cfg=proc_cfg, dqn_cfg=dqn_cfg,
        sched_cfg=SchedulerConfig(update_interval=1, step=0.05),
        train_cfg=TrainingConfig(), callbacks=cb, device="cpu", seed=0,
    )


def test_rnd_disabled_no_module():
    runner = _make_runner(rnd_enabled=False)
    assert runner.rnd is None


def test_rnd_enabled_instantiates_module():
    runner = _make_runner(rnd_enabled=True, rnd_warmup_steps=0)
    assert runner.rnd is not None


def test_rnd_run_end_to_end_and_logs_result():
    logs: list[str] = []
    runner = _make_runner(
        on_log=lambda level, msg: logs.append(msg),
        rnd_enabled=True, rnd_warmup_steps=0, rnd_beta=0.2,
    )
    runner.run()
    assert any("RND_RESULT" in m and "ratio_int_ext=" in m for m in logs)


def test_rnd_disabled_emits_no_rnd_result():
    logs: list[str] = []
    runner = _make_runner(on_log=lambda level, msg: logs.append(msg), rnd_enabled=False)
    runner.run()
    assert not any("RND_RESULT" in m for m in logs)
```

- [ ] **Step 2: Lancer pour vérifier l'échec**

Run: `pytest tests/training/test_rnd_runner.py -q`
Expected: FAIL (`AttributeError: ... 'rnd'`).

- [ ] **Step 3: Wirer le runner**

Dans `mw_ia/training/runner.py` :

(a) En tête de fichier (zone imports, après les imports existants), ajouter :

```python
from mw_ia.neural.rnd import RNDModule
```

(b) Dans `ConvRecurrentProceduralDQNRunner.__init__`, après la ligne `self._ep_to_diff_030: int | None = None` (ligne 659), ajouter :

```python
        if dqn_cfg.rnd_enabled:
            self.rnd: RNDModule | None = RNDModule(
                in_channels=in_channels, rows=proc_cfg.max_rows, cols=proc_cfg.max_cols,
                embed_dim=dqn_cfg.rnd_embed_dim, lr=dqn_cfg.rnd_lr,
                clip=dqn_cfg.rnd_clip, warmup_steps=dqn_cfg.rnd_warmup_steps,
                device=device, seed=seed,
            )
        else:
            self.rnd = None
        self._rnd_int_sum: float = 0.0
        self._rnd_ext_sum: float = 0.0
        self._rnd_last_loss: float = 0.0
        self._rnd_warned: bool = False
```

(c) Dans `run()`, dans la boucle interne, juste APRÈS le bloc qui calcule `next_obs` (lignes 730-734) et AVANT `self.agent.observe(...)` (ligne 735), insérer :

```python
                if self.rnd is not None:
                    bonus = self.rnd.compute_bonus(next_obs)
                    self._rnd_last_loss = self.rnd.update(next_obs)
                    self._rnd_ext_sum += abs(r)
                    self._rnd_int_sum += self.dqn_cfg.rnd_beta * bonus
                    r = r + self.dqn_cfg.rnd_beta * bonus
                    if (
                        not self._rnd_warned
                        and self._rnd_ext_sum > 0.0
                        and self._rnd_int_sum / self._rnd_ext_sum > self.dqn_cfg.rnd_ratio_warn
                    ):
                        self._rnd_warned = True
                        self.callbacks.fire_log(
                            "warning",
                            f"RND ratio int/ext > {self.dqn_cfg.rnd_ratio_warn} "
                            f"(run potentiellement contamine)",
                        )
```

(d) À la toute fin de `run()`, APRÈS le bloc `BX_PROBE_RESULT` (après la ligne 808), ajouter :

```python
        if self.rnd is not None:
            ratio = (
                self._rnd_int_sum / self._rnd_ext_sum if self._rnd_ext_sum > 0.0 else 0.0
            )
            self.callbacks.fire_log(
                "info",
                f"RND_RESULT rnd_beta={self.dqn_cfg.rnd_beta} "
                f"ratio_int_ext={ratio:.3f} predictor_loss={self._rnd_last_loss:.4f}",
            )
```

IMPORTANT : ne toucher QUE `ConvRecurrentProceduralDQNRunner`. Le bonus RND est ajouté à `r` AVANT `agent.observe` (l'agent apprend le reward augmenté) et avant `ep_reward += r`. Quand `rnd is None`, aucun calcul, aucun log → no-op strict.

- [ ] **Step 4: Lancer pour vérifier le succès**

Run: `pytest tests/training/test_rnd_runner.py -q`
Expected: PASS (4 tests).

- [ ] **Step 5: Non-régression**

Run: `pytest tests/ -q`
Expected: tous verts (aucun runner autre que V2-ZY touché).

- [ ] **Step 6: Commit**

```bash
git add mw_ia/training/runner.py tests/training/test_rnd_runner.py
git commit -m "feat(v2-c0): wire RNDModule in ConvRecurrent runner + RND_RESULT log"
```

---

## Task 6 : Flags CLI `--rnd` + hyperparams

**Files:**
- Modify: `scripts/train_cnn_lstm_dqn_procedural.py` (argparse avant `args = parser.parse_args()` ; construction `ConvRecurrentDQNConfig`)

- [ ] **Step 1: Ajouter les arguments argparse**

Dans `scripts/train_cnn_lstm_dqn_procedural.py`, juste avant `args = parser.parse_args()`, ajouter :

```python
    parser.add_argument(
        "--rnd",
        action=argparse.BooleanOptionalAction,
        default=False,
        help="V2-C0 : exploration intrinseque RND (single-stream, bonus appris "
             "ajoute au reward). Default False = pas de bonus.",
    )
    parser.add_argument("--rnd-beta", type=float, default=0.5,
                        help="V2-C0 : coefficient du bonus RND (default 0.5).")
    parser.add_argument("--rnd-lr", type=float, default=1e-4,
                        help="V2-C0 : learning rate du predictor RND (default 1e-4).")
    parser.add_argument("--rnd-embed-dim", type=int, default=128,
                        help="V2-C0 : dim de l'embedding target/predictor (default 128).")
    parser.add_argument("--rnd-clip", type=float, default=5.0,
                        help="V2-C0 : plafond per-step du bonus normalise (default 5.0).")
    parser.add_argument("--rnd-warmup-steps", type=int, default=1000,
                        help="V2-C0 : pas ou le bonus reste 0 le temps que les "
                             "normaliseurs se calent (default 1000).")
    parser.add_argument("--rnd-ratio-warn", type=float, default=10.0,
                        help="V2-C0 : seuil de WARN du ratio intrinsec/extrinsec "
                             "(default 10.0).")
```

- [ ] **Step 2: Wirer dans la config**

Dans la construction de `ConvRecurrentDQNConfig(...)`, après `b1a_mix_ratio=args.b1a_mix_ratio,` (ou la dernière ligne BX si présente), ajouter :

```python
        rnd_enabled=args.rnd,
        rnd_beta=args.rnd_beta,
        rnd_lr=args.rnd_lr,
        rnd_embed_dim=args.rnd_embed_dim,
        rnd_clip=args.rnd_clip,
        rnd_warmup_steps=args.rnd_warmup_steps,
        rnd_ratio_warn=args.rnd_ratio_warn,
```

- [ ] **Step 3: Smoke manuel CPU**

Run:
```bash
python scripts/train_cnn_lstm_dqn_procedural.py --episodes 3 --mode obstacles \
    --device cpu --max-rows 5 --max-cols 5 --max-steps 20 --sequence-length 10 \
    --no-eval --rnd --rnd-warmup-steps 0
```
Expected: se termine sans erreur, affiche une ligne `RND_RESULT rnd_beta=0.5 ratio_int_ext=... predictor_loss=...`.

- [ ] **Step 4: Vérifier `--help` ASCII**

Run: `python scripts/train_cnn_lstm_dqn_procedural.py --help`
Expected: affiche `--rnd`, `--rnd-beta`, etc. sans `UnicodeEncodeError`.

- [ ] **Step 5: Commit**

```bash
git add scripts/train_cnn_lstm_dqn_procedural.py
git commit -m "feat(v2-c0): CLI flags --rnd + 6 hyperparams"
```

---

## Task 7 : Smoke CI

**Files:**
- Modify: `.github/workflows/aether_verify.yml` (2 steps après les smokes BX)

- [ ] **Step 1: Repérer le pattern existant**

Run: `grep -n "bx-repr-oracle\|Smoke test V2-BX" .github/workflows/aether_verify.yml`
Expected: localiser les 2 steps smoke V2-BX (pour imiter leur structure et leur indentation).

- [ ] **Step 2: Ajouter les 2 steps smoke RND**

Dans `.github/workflows/aether_verify.yml`, juste après le dernier step smoke V2-BX, ajouter (en respectant l'indentation YAML exacte du fichier) :

```yaml
      - name: Smoke test V2-C0 RND (single-stream)
        run: |
          mkdir -p checkpoints
          python scripts/train_cnn_lstm_dqn_procedural.py --episodes 10 --mode obstacles \
            --device cpu --max-rows 5 --max-cols 5 --max-steps 20 --sequence-length 10 \
            --no-eval --rnd --rnd-warmup-steps 0

      - name: Smoke test V2-C0 RND + Polyak cohabit
        run: |
          mkdir -p checkpoints
          python scripts/train_cnn_lstm_dqn_procedural.py --episodes 10 --mode obstacles \
            --device cpu --max-rows 5 --max-cols 5 --max-steps 20 --sequence-length 10 \
            --no-eval --rnd --rnd-warmup-steps 0 --polyak-tau 0.005
```

- [ ] **Step 3: Valider le YAML**

Run: `python -c "import yaml; yaml.safe_load(open('.github/workflows/aether_verify.yml')); print('YAML OK')"`
Expected: `YAML OK`.

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/aether_verify.yml
git commit -m "ci(v2-c0): smoke jobs RND single-stream + Polyak cohabit"
```

---

## Task 8 : Vérification e2e + reproductibilité no-op

**Files:** aucun (vérification)

- [ ] **Step 1: Suite complète**

Run: `pytest -q`
Expected: **402 passed** (384 baseline + 4 RMS + 2 net + 6 module + 6 config + 4 runner = +22 ; le compte exact peut varier de ±2, l'essentiel : 0 failed).

- [ ] **Step 2: Reproductibilité baseline (no-op)**

Run A (sans RND) :
```bash
python scripts/train_cnn_lstm_dqn_procedural.py --episodes 20 --mode obstacles \
    --device cpu --max-rows 5 --max-cols 5 --max-steps 20 --sequence-length 10 \
    --no-eval --seed 0
```
Run B (flags RND aux défauts, mais `--no-rnd`) :
```bash
python scripts/train_cnn_lstm_dqn_procedural.py --episodes 20 --mode obstacles \
    --device cpu --max-rows 5 --max-cols 5 --max-steps 20 --sequence-length 10 \
    --no-eval --seed 0 --no-rnd
```
Expected : la ligne `Final : winrate=..., difficulty=...` est **identique** entre A et B, et aucune ligne `RND_RESULT` n'apparaît (RND désactivé = no-op strict). Si A != B → FAILURE.

- [ ] **Step 3: Aether smoke inchangé**

Run: `bash aether/verify_all.sh`
Expected: `8 OK`, exit 0.

- [ ] **Step 4: Commit (si ajustement nécessaire)**

Cette tâche est vérification-only. Si tout passe, rien à committer — juste rapporter.

---

## Task 9 : Bench Phase 1 (n=3, 15×15, GPU) — GATE

> **Runs GPU manuels** (RTX 3060), pas du code. Chaque run ≈ 0,75 h. Suit le protocole spec §7.

**Files:**
- Create: `docs/findings/2026-06-06-v2c0-rnd-results.md` (table de résultats)

- [ ] **Step 1: Lancer RND n=3 (défauts)**

```bash
for seed in 0 1 2; do
  python scripts/train_cnn_lstm_dqn_procedural.py \
    --episodes 5000 --mode obstacles --device cuda --seed $seed \
    --max-rows 15 --max-cols 15 --max-steps 400 --replay-capacity 2500 \
    --polyak-tau 0.005 --max-attempts-bfs 500 \
    --rnd \
    --best-checkpoint-path checkpoints/c0_rnd_seed${seed}.pt \
    2>&1 | tee logs/c0_rnd_seed${seed}.log
done
```

- [ ] **Step 2: Collecter `diff_max`, `best_eval`, `ratio_int_ext`**

Run: `grep -hE "BX_PROBE_RESULT|RND_RESULT" logs/c0_rnd_seed*.log`
Reporter par seed dans `docs/findings/2026-06-06-v2c0-rnd-results.md`, comparé aux 2 références (baseline 0.36/64 %, count-based B@0.02 0.40/66.7 %).

- [ ] **Step 3: Appliquer le GATE Phase 1**

```text
PASSE si : diff_max moy > 0.40 OU best_eval moy > 66.7 %  -> Task 10 (Phase 2 n=5)
SINON : finding neutre/negatif. Re-essai rapide (β plus bas, ex 0.2, ou warmup
        plus long) si le ratio int/ext montre une contamination ; sinon conclure.
```

- [ ] **Step 4: Commit de la table**

```bash
git add docs/findings/2026-06-06-v2c0-rnd-results.md
git commit -m "bench(v2-c0): RND n=3 15x15 Phase 1 results"
```

---

## Task 10 : Bench Phase 2 (n=5) + finding + tag

**Files:**
- Modify: `docs/findings/2026-06-06-v2c0-rnd-results.md`, `CLAUDE.md`, mémoire

- [ ] **Step 1: Phase 2 n=5 (si Phase 1 passe)**

Relancer sur seeds 0-4 (mêmes flags que Phase 1 gagnante), best-checkpoint distinct par seed. Collecter `best_eval` et `diff_max`.

```text
SUCCES si : best @ diff=0.30 moy >= 74 % OU diff_max moy > 0.45
```

- [ ] **Step 2: Finding consolidé**

Compléter `docs/findings/2026-06-06-v2c0-rnd-results.md` : tables n=3 (+n=5), comparaison aux 2 références, ratio int/ext observé, verdict (RND aide / neutre / corrompt), et prochaine étape (two-stream V2-C1, sampling buffer, ou autre selon outcome).

- [ ] **Step 3: CLAUDE.md + mémoire**

Section « V2-C0 — RND » au CLAUDE.md (statut, verdict, défauts, suite), table sous-projets, compteurs de tests, liste de tags. Mettre à jour `MEMORY.md` + fichier mémoire d'exécution V2-C0.

- [ ] **Step 4: Tag**

```bash
pytest -q   # confirmer vert avant tag
git add -A && git commit -m "docs(v2-c0): finding consolide + CLAUDE.md + memoire"
git tag v0.2.0-c0
```

---

## Self-Review (rempli par l'auteur du plan)

- **Couverture spec** : §3 single-stream → Task 5 (bonus additif au reward) ; §4.1 RNDModule → Tasks 1-3 ; §4.2 flags → Task 4 ; §4.3 runner → Task 5 ; §4.4 CLI → Task 6 ; §5 anti-β (obs-norm, bonus-norm, clip, warmup, β défaut, ratio/WARN) → Tasks 1,3,5 ; §6 testing → toutes ; §7 bench → Tasks 9-10 ; §8 livrable/tag → Task 10. ✅
- **No-op strict** : Task 8 Step 2 (A==B, pas de RND_RESULT quand off). ✅
- **Cohérence des noms** : `RunningMeanStd`, `_RNDNet`, `RNDModule.compute_bonus/update`, champs `rnd_enabled/rnd_beta/rnd_lr/rnd_embed_dim/rnd_clip/rnd_warmup_steps/rnd_ratio_warn`, `self.rnd`, ligne `RND_RESULT` — identiques entre module, config, runner, CLI et tests. ✅
- **Placeholders** : aucun ; tout step de code contient le code complet. Tasks 9-10 explicitement runs GPU manuels avec commandes exactes. ✅
- **Pièges projet** : help-text/erreurs ASCII (pas de β/τ littéraux) ; `--sequence-length ≤ max_steps` sur petites grilles (découverte BX) ; `--max-attempts-bfs 500` au bench ; modules touchés (config, runner, script) sous flags conditionnels ; `ConvRecurrentProceduralDQNRunner` seul runner modifié. ✅
- **Garde-fou anti-β** : normalisation (Task 1+3) + clip (Task 3) + warmup (Task 3) + ratio log/WARN sans abort ni auto-decay (Task 5). ✅
